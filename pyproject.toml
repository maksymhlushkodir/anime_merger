[project]
name = "anime-merger"
version = "0.1.0"
description = "Merge audio tracks and subtitles from multiple anime releases."
requires-python = ">=3.11"

dependencies = [
    "click"
]

[project.scripts]
anime-merger = "anime_merger.cli:main"